To run tests on GET requests, use the following curl command:
curl -g -G --data-urlencode @query-simple.json  http://localhost:8888/query

replace query-simple.json with the filename of the test.  DO NOT get rid of the @ symbol.
replace 'query' with whatever url you're testing.
Important note: all get tests must be in the following format:
q=<JSON STRING>

To run tests on POST requests, use the following curl command:
curl -d @collect-simple.json http://localhost:8888/collect
replace collect-simple.json with the filename of the test.  DO NOT get rid of the @ symbol.
replace 'collect' with whatever url you're testing.

